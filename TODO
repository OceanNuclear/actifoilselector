----code accuracy improvement (and high priority maintenance/code quality issues)
[x]Fix git commit hooks
[x]Merged rows: how do we deal with their scale factors???
[x]speed up corresponding_background_level
[x]Populate the covariance matrix in get_precision correctly, rather than simply using the inverse of the variance in counts as the main diagonal.
[]Deal with double-counting of some reactions,
    - example: see [i for i in xs_dict.keys() if i.startswith("Pd106")]:
    - "Pd106-Rh105-MT=28"
    - "Pd106-Rh105-MT=(650-669)"
    - "Pd106-Ru103-MT=107"
    - "Pd106-Ru103-MT=(800-819,849)"
    - use "~/Others/FoilSelector/JEFF/JEFF33-n/endf6/46-Pd-106g.jeff33" compared to another library's 46-Pd-106.
    - This can be solved by `get_reaction_components(mt)` specified by https://docs.openmc.org/en/latest/pythonapi/generated/openmc.data.IncidentNeutron.html#openmc.data.IncidentNeutron.get_reaction_components
[x]Change the name of `Integrate` to `Intergral`
[x]Instead of a flat distribution, use a better (Klein-Nishina) simulation of the Compton continuum
[x]Tab1DExtended: Addition and multiplication
[]Merge peak should give a list of peaks (depending on how many is detectible) of {0<=len(output_peaks)<len(peak_group)} rather than a single peak.
----code maintainability (specific)
[]Make the foilselector write to a .csv instead of five separate .jsons?
[]Put the following file names all in foldermanagement.py:
    [].atomic_composition.json
    [].gs.csv
    [].continuous_apriori.csv
    [].response_matrices.json
    [].background_response_matrices.json
    [].effective_response_matrices.json
    []Add the list of radiations into f"gamma_spectra/{foil_name}.json" (might need to re-jig the list saving files) as key="peaks", and don't save it as .effective_peaks.json?
        - Or as a separate .csv file.
    [].effective_peaks.json
[]Kick out unused (deprecated) functions
    - using vulture
    - foilselector.openmcextension.extended_io.load_endf_directories()

----Nice-to-have physics features
[x]Apply resolution curve blurring to the Compton Edge.
[]Improve Tab1DExtended's multiplication with another Tab1D? because the current scheme does not take into account the interpolation scheme.
[]Incorporate pair-production effects? And multiple Compton scatter effects?

----Other code maintainability/quality (generic)
[]use pathlib.Path instead of accepting strings
[]Tests/Unittest
[]CI: https://carpentries-incubator.github.io/python-intermediate-development/23-continuous-integration-automated-testing/index.html
[]Sphinx
[]Step4: copy of step2-3, but uses the true irradiation schedule, where each foil's irradiation times differ.
    []Takes in the following extra arguments:
        []--irradiation-schedule into the distribution
            - Make a document describing the syntax of the --irradiation-schedule file
            - location of such a sample: ~/Self/FoilSelector/NewChipIR/PostHoc/irradiation_schedule.txt
            []Separate efficiency curve for each sample included inside the file(?)
        []--mass-file to specify the mass of each foil
    []Also calculate the activity at time d, where d>c, used for health physics reasons.
    []Calculates which reactions may require self-shielding corrections
